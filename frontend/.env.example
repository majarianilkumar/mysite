# Development — leave empty, Vite proxy handles /api → localhost:4000
VITE_API_URL=

# Production — set this to your deployed backend URL (no trailing slash)
# Example: https://john-doe-portfolio-api.onrender.com
# VITE_API_URL=https://your-backend.onrender.com
